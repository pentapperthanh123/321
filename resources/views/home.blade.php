@include('partials/header')

	<main>
        @yield('main')
    </main>

    <!-- footer -->
    @include('partials/footer')

    <!-- Endfooter -->

	<!-- form itself end-->
    
    @include('partials/form')

    <!-- Endform itself end-->
